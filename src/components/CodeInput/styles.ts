import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  root: {
    marginTop: '40px',
    display: 'flex',
    justifyContent: 'center'
  },
  description: {
    textAlign: 'center',
    marginBottom: '6px'
  }
});
