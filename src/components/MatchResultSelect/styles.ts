import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  container: {
    position: 'relative'
  },
  matchColor: {
    fontSize: '10px',
    color: 'gray',
    position: 'absolute',
    left: '31px',
    top: '28px'
  }
});
