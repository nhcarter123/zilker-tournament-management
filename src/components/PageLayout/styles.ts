import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  root: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    height: '98vh'
  },
  container: {
    width: '280px'
  },
  itemContainer: {
    padding: '0 20px',
    margin: '16px 0'
  },
  button: {
    width: '100%'
  }
});
