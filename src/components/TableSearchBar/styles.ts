import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  root: {
    display: 'flex',
    position: 'sticky',
    top: '60px',
    padding: '6px',
    zIndex: 1
  },
  searchbar: {
    // width: '404px'
  }
});
