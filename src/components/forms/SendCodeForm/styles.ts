import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  phoneInput: {
    '& > input': {
      height: '38px',
      fontSize: '20px',
      border: '1px solid',
      borderColor: '#b1b1b1',
      borderRadius: '2px'
    }
  }
});
